// This file was auto-generated by SourceMod (v1.11.0.6825)
// ConVars for plugin "extend/l4d_safe_door_spam.smx"


// 0=Plugin off, 1=Plugin on.
// -
// Default: "1"
l4d_safe_spam_allow "1"

// 0.0=Off. How many seconds after round start (or after unlocking by l4d_safe_spam_lock* and l4d_safe_spam_lock* cvars) until the locked saferoom door will automatically fall. Unless manually opened.
// -
// Default: "0.0"
l4d_safe_spam_fall_time "0.0"

// 0=Off. 1=Display a message showing who opened or closed the saferoom door. 2=Display a message when saferoom door is auto unlocked (_touch and _lock cvars). 3=Both.
// -
// Default: "0"
l4d_safe_spam_hint "0"

// Final door state on round start: 0=Use map default. 1=Close last door. 2=Open last door.
// -
// Default: "0"
l4d_safe_spam_last "0"

// 0.0=Off. How many seconds after round start will the saferoom door remain locked.
// -
// Default: "0.0"
l4d_safe_spam_lock "0.0"

// 0.0=Off. How many seconds after round start will the saferoom door remain locked. For the second+ round of a map.
// -
// Default: "0.0"
l4d_safe_spam_lock_2 "0.0"

// Turn on the plugin in these game modes, separate by commas (no spaces). (Empty = all).
// -
// Default: ""
l4d_safe_spam_modes ""

// Turn off the plugin in these game modes, separate by commas (no spaces). (Empty = none).
// -
// Default: ""
l4d_safe_spam_modes_off ""

// Turn on the plugin in these game modes. 0=All, 1=Coop, 2=Survival, 4=Versus, 8=Scavenge. Add numbers together.
// -
// Default: "0"
l4d_safe_spam_modes_tog "0"

// 0=Off, 1=Keep the first saferoom door open once opened, 2=Make the first saferoom door fall once opened.
// -
// Default: "2"
l4d_safe_spam_open "2"

// 0.0=Always has physics. How many seconds until the fallen doors physics are disabled.
// -
// Default: "3.0"
l4d_safe_spam_physics "3.0"

// 0=Map default. 1=Classic. 2=Last Stand. Which door model to use on the first and last saferooms.
// -
// Default: "0"
l4d_safe_spam_skin "0"

// How many seconds to block after closing the last saferoom door.
// -
// Default: "0.0"
l4d_safe_spam_time_close "0.0"

// How many seconds to block after opening the last saferoom door.
// -
// Default: "0.0"
l4d_safe_spam_time_open "0.0"

// 0.0=Off. How many seconds after attempting to open the locked saferoom door until it will unlock (overrides the _time cvar).
// -
// Default: "0.0"
l4d_safe_spam_touch "0.0"

// 0.0=Off. How many seconds after attempting to open the locked saferoom door until it will unlock (overrides the _time cvar). For the second+ round of a map.
// -
// Default: "0.0"
l4d_safe_spam_touch_2 "0.0"

// 0=Off. When the last saferoom door is used enable the timeout on: 1=Open, 2=Close, 3=Both.
// -
// Default: "3"
l4d_safe_spam_type "3"


